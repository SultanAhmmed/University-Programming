#include <bits/stdc++.h>

#define ll long long
using namespace std;

void solve() {

	ll prime;
	cin >> prime;

	cout << '2' << " " << prime - 1 << '\n';

}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0); cout.tie(0);

	int tests;
	cin >> tests;
	while (tests--) {
		solve();
	}
}
