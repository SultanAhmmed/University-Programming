#include<stdio.h>
int main()
{
    double A,B,C;
    scanf("%lf %lf %lf",&A,&B,&C);
    double perimetro = A + B + C;
    double area = .5*(A+B)*C;
    if(((A+B)>C) && ((B+C)>A) && ((A+C)>B)){
        printf("Perimetro = %.1lf\n",perimetro);
    }
    else{
        printf("Area = %.1lf\n",area);
    }

    return 0;
}
